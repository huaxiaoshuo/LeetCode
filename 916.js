/**
 * @param {string[]} A
 * @param {string[]} B
 * @return {string[]}
 */
var wordSubsets = function(A, B) {
    let coord = {}
    B.map(itemB => {
        for (let a = 0; a < A.length; a++) {
            if (!coord[A[a]]) {

            }
        }
    })
};
const A = ["amazon","apple","facebook","google","leetcode"], B = ["e","o"]
console.error(wordSubsets(A, B))