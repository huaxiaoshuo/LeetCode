/**
 * @param {number[]} a
 * @param {number[]} b
 * @return {number}
 */
var smallestDifference = function(a, b) {
    a = a.sort((a, b) => a - b)
    b = b.sort((a, b) => a - b)
    let left = 0, right = 0
    while (left < a.length && right < b.length) {

    }
};
const a = [1, 3, 15, 11, 2], b = [23, 127, 235, 19, 8]