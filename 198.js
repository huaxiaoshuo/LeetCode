/**
 * @param {number[]} nums
 * @return {number}
 * 可以隔一个屋子   也可以隔两个屋子
 * 
 *  
 */
var rob = function(nums) {
    let maxNum = 0,
    maxNum2 = 0
    for (let a = 0; a < nums.length; a++) {
        
    }
};

// 

console.error(rob([2,7,9,3,1]))