/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var numSubarrayProductLessThanK = function (nums, k) {
    let result = [],
        findResult = function () {

        };
    nums.map((item) => {
        let sum = 1,
            length = 1;


    })

};
numSubarrayProductLessThanK([10, 5, 2, 6], 100)
