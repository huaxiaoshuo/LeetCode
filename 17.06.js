/**
 * @param {number} n
 * @return {number}
 */
var numberOf2sInRange = function(n) {
    let result = 0
    for (let a = 0; a < n; a++) {
        // let indexArr = (a + 1 + '').split('').filter((item) => {return item === '2'})
        // result += indexArr.length
        console.error(a)
    }
    return result
};
// 559366752
console.error(numberOf2sInRange(559366752))